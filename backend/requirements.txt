# Core dependencies
agno>=0.8.0
fastapi>=0.95
uvicorn[standard]>=0.22
httpx>=0.24
pydantic>=2.0
aiohttp>=3.8.0
python-dotenv>=1.0
pydantic>=2.0
chromadb>=0.4
rich>=12.6
async-timeout>=4.0
requests>=2.31.0

# Testing
pytest>=7.4
pytest-asyncio>=0.21

# Optional / recommended
psycopg2-binary>=2.9  # if using Postgres for Agno storage

# Optional: Web Search (install with pip install agno[tavily] agno[exa])
# tavily-python>=0.3.0  # Uncomment to enable Tavily search
# exa-py>=1.0.0         # Uncomment to enable Exa search
